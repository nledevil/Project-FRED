[Unit]
Description=Restart servo-server when it is running but not answering

[Service]
Type=oneshot
Environment=UNIT=servo-server
# /api/health answers before the token gate, by design — see servo_server.py.
Environment=URL=http://127.0.0.1:8082/api/health
ExecStart=/usr/local/sbin/service-watchdog
