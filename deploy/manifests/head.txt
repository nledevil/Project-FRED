# Head Pi — servo bus (PCA9685 over I2C), CSI camera, status LED.
#
# Everything the brain does NOT do here. This machine stopped being the brain on
# 2026-08-06; what is left is three device servers. Anything in the repo that is
# not listed below has no business on this Pi.
#
# root:    where the tree lands on the target
# flatten: no  -> repo paths are preserved (deploy/x.py -> <root>/deploy/x.py)
# seed:    copy only if absent — never clobber the live copy
# keep:    exists on the target, is not deployed, must never be touched

root: /home/dietpi/inmoov
flatten: no

# --- the three services this Pi actually runs -------------------------------
deploy/servo_server.py
deploy/camera_stream.py
deploy/led_server.py

# --- unit files, for reinstalling from scratch ------------------------------
deploy/servo-server.service
deploy/camera-stream.service
deploy/led-server.service

# NOT shipped: deploy/announce_ip.py. It spoke the Pi's DHCP address aloud back
# when the head was the brain, and it imports netinfo + settings + sound — three
# modules with no other reason to be on this machine, one of which needs audio
# hardware the head no longer has. Its address is now fixed by the NUC's DHCP
# anyway. inmoov-announce-ip.service was disabled 2026-08-09 to match.

# --- the shared package, closure only ---------------------------------------
# servo_server imports servo_controller; led_server imports led. That is the
# whole of it — camera_stream imports picamera2 directly and nothing from here.
# Both hardware libs (adafruit_servokit, RPi.GPIO) are imported lazily inside
# the modules, so neither is needed to import the package.
inmoov/__init__.py
inmoov/servo_controller.py
inmoov/led.py

# --- diagnostics ------------------------------------------------------------
# Pi-only (no vcgencmd on x86); this build has a history of thermal throttling.
deploy/vcgencmd_power_report.sh

# --- config -----------------------------------------------------------------
# This Pi is the AUTHORITY for servo calibration: servo_server reads it at
# startup, and RemoteServoController pulls limits from here rather than trusting
# the brain's copy. The panel's /api/save pushes changes over HTTP, so a deploy
# must not overwrite it — a stale repo copy would silently undo a calibration.
seed: config/servos.json

# --- never touch ------------------------------------------------------------
# No .git here: as of 2026-08-09 this directory is a deploy target, not a
# checkout. Stripping it left ~50 tracked files "deleted", which is one stray
# `git checkout .` away from undoing the strip — and a second checkout is how
# the branches drift apart in the first place. Deploy from the NUC.
keep: venv
keep: __pycache__
